package views;

import controllers.ProfileController;

import javax.swing.*;

public class ChangePasswordView extends JFrame {

    public ChangePasswordView(ProfileController controller) {
        setTitle("Change Password");
        setSize(300, 200);
        setLayout(null);

        setVisible(true);
    }
}
